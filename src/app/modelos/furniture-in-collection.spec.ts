import { FurnitureInCollection } from './furniture-in-collection';

describe('FurnitureInCollection', () => {
  it('should create an instance', () => {
    expect(new FurnitureInCollection()).toBeTruthy();
  });
});
